# Greyhaven Phone v2.8.1 — Life/Clock Integration Fix

Use this over v2.8.0.

Fixes:
- Public `openLife()` now opens the Phone first when the Phone is closed.
- Greyhaven Life floating clock can reliably open Phone -> Life -> Clock.
- Life folder preview now visibly includes Clock + Garage + Property.
- Adds public `openClock()` helper.
- No Phone app features were removed or reset.

Replace the same v2.8 overlay files with the files in this ZIP.
Your core `index.js`, `bridge.js` and `style.css` remain untouched.
